import LoginForm from './components/login-form'

const LoginPage = () => {
	return (
		<div className='fixed inset-0 flex items-center justify-center bg-gradient-to-br from-blue-50 via-white to-slate-100'>
			<LoginForm />
		</div>
	)
}

export default LoginPage
